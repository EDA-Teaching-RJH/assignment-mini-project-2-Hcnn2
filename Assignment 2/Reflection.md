For my main feature functions, examples of where the code is learnt comes from is mainly leactures 2, 3 and 4. These lectures detail how to use conditionals, logic and loops. Ive used these many times before in the corresponding workshops so referring back to those was a big help when developing these functions. Other examples of course material used in my code come from lists, libraries, file I/O and OOP. These come from lectures 6-9, correspondingly. After the initial database, display menu and main function i decided to start with OOP first to establish the classes before moving onto File I/O. This wasnt that difficult but i had to make a way for the program to run without information in the csv, before the first save. I had researched how to get the fexible search to work but it still took and while to get working. Finally the testing, which i had written some initial tests to check for things like wrong case, empty lists and invalid formats.